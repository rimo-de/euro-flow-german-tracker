
export type UserSettings = {
  autoVat: boolean;
  autoBackup: boolean;
  currencyDisplay: boolean;
  manualVat: boolean; // We'll keep this for backward compatibility, but it won't be used actively
};
